import React, { FC } from "react";
import { ITeamSectionProps } from "./types";
import { TeamContainer } from "./Container";

export const TeamSection: FC<ITeamSectionProps> = () => {
    return (
        <TeamContainer />
    )
}